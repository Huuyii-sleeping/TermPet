## MODIFIED Requirements

### Requirement: 桥接服务必须提供最小动作请求接口

桥接服务 SHALL 提供统一动作请求入口，用于接收桌面端发起的动作请求、返回标准动作结果，并写入本地动作审计记录。

#### Scenario: 动作请求到达

- **WHEN** 桌面端向桥接服务提交动作请求
- **THEN** 桥接服务必须校验动作与事件关系、返回标准结果，并记录审计摘要
