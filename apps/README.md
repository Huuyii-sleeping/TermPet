# apps

`apps/` 存放第一版可直接运行的应用。

当前包含：

- `desktop/`：Electron 桌面端，负责桌宠窗口、状态展示和交互面板。
- `bridge/`：本地桥接服务，负责接收标准事件、管理最近历史并向桌面端广播。

目录边界：

- 这里的模块是“可运行应用”，不是共享库。
- 共享类型、协议和插件接口应放在 `packages/`。
- 面向具体工具的适配逻辑应优先放在 `plugins/`。

推荐阅读顺序：

1. `desktop/README.md`
2. `bridge/README.md`

